import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Left,SmallLeft, Right,SmallRight } from './ButtonLink.stories';
import ButtonLink from './ButtonLink'


<Meta title="Atoms | ButtonLink" component={Left} />

# ButtonLink

This docs talk about all variant of `ButtonLink` component.  You can define position type with: `left` and `right`.

<Preview>
  <Story name="left">
    <div style={{display:"flex",width:"200px",justifyContent:"space-between"}}>
    <Left onClick={()=>alert("hello")} />
    <SmallLeft onClick={()=>alert("hello")} />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name="right">
    <div style={{display:"flex", width:"200px",justifyContent:"space-between"}}>
     <Right onClick={()=>alert("hello")}/>
     <SmallRight onClick={()=>alert("hello")}/>
    </div>
  </Story>
</Preview>

Copied this code directly !

  ```javascript
  <ButtonLink
    position="left"
    width="default"
    height="default"
    onClick={()=>alert("hello")}/>
  ```

# Props

<Props of={ButtonLink} />