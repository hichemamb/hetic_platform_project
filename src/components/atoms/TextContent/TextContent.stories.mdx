import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Default } from './TextContent.stories';
import TextContent from './TextContent'


<Meta title="Atoms | TextContent" component={Default} />

# TextContent

This docs talk about all variant of `TextContent` component.  You can define position type with: `left` and `right`.

<Preview>
  <Story name="Default">
      <Default />
  </Story>
</Preview>

Copied this code directly !

  ```javascript
  <TextContent>
   your text content here !
  </TextContent>
  ```

# Props

<Props of={TextContent} />