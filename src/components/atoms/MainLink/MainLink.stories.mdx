import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Default } from './MainLink.stories';
import MainLink from './MainLink'


<Meta title="Atoms | MainLink" component={Default} />

# MainLink

This docs talk about `MainLink` component. Check the code in clicked to Show code for more details. 

<Preview>
  <Story 
    name="Principle"
    parameters={{
        design: {
        type: 'figma',
        url: 'https://www.figma.com/file/8RQyaZMzX69cDcejSonfEb/Projet-Hetic-SCHOOL'
    }
    }}
    >
      <Default onClick={()=>alert("Hello")}> Default </Default>
  </Story>
</Preview>

Copied this code directly !

  ```javascript
  <MainLink onClick={()=>alert("Hello")}>Default</MainLink>
  ```

# Props

<Props of={MainLink} />