import React from 'react';
import TitleGroup from './TitleGroup';

export const TitleGroupDefault = ({title, subText, size, sizeSubText}) => (
    <TitleGroup title={title} subText={subText} size={size} sizeSubText={sizeSubText} />
);